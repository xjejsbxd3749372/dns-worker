{
  "name": "dns-worker",
  "version": "1.0",
  "private": false,
  "description": "Protective DoH Resolver based on Cloudflare Workers",
  "scripts": {
    "dev": "npm run db:migrate:dev && concurrently \"npm run ui:dev\" \"wrangler dev\"",
    "start": "npm run build && wrangler dev",
    "start:serverfull": "npm run build && tsx src/serverfull/index.ts",
    "serverfull": "npm run build && tsx src/serverfull/index.ts",
    "service-create:linux": "tsx scripts/create-linux-service.ts",
    "deploy": "npm run build && npm run db:migrate:prod && wrangler deploy",
    "deploy:skipdb": "npm run build && wrangler deploy",
    "build": "npm run ui:build",
    "build:pages": "npm run ui:build && npx esbuild src/index.ts --bundle --outfile=static/_worker.js --platform=neutral --format=esm --minify --external:cloudflare:*",
    "postinstall": "npm run ui:install",
    "db:setup": "wrangler d1 create dns_worker_db",
    "db:migrate:dev": "wrangler d1 migrations apply dns_worker_db --local",
    "db:migrate:prod": "echo \"y\" | wrangler d1 migrations apply dns_worker_db --remote",
    "db:status": "wrangler d1 execute dns_worker_db --local --command='SELECT name FROM sqlite_master WHERE type=\"table\"'",
    "test": "tsx test/test_legacy_compat.ts",
    "test:compat": "tsx test/test_legacy_compat.ts",
    "test:serverfull": "tsx test/test_serverfull.ts",
    "lint": "eslint . --ext .ts,.tsx",
    "ui:dev": "cd web && npm run dev",
    "ui:build": "cd web && npm run build",
    "ui:install": "cd web && npm install -f"
  },
  "keywords": [
    "dns",
    "cloudflare-workers",
    "doh"
  ],
  "author": "Hugo Koo",
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "@types/node": "^26.5.1",
    "concurrently": "^10.0.5",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.1"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
